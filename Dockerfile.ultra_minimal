# Callytics Ultra Minimal Dockerfile (CPU 전용)
FROM python:3.10-slim

# 환경 변수 설정
ENV PYTHONUNBUFFERED=1
ENV DEBIAN_FRONTEND=noninteractive

# 시스템 패키지 업데이트 및 필수 패키지 설치 (최소화)
RUN apt-get update && apt-get install -y \
    ffmpeg \
    libsndfile1 \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

# 작업 디렉토리 설정
WORKDIR /app

# Python 의존성 설치 (Ultra Minimal)
COPY requirements_ultra_minimal.txt .
RUN pip install --no-cache-dir -r requirements_ultra_minimal.txt

# 한국어 처리 패키지 설치
RUN pip install --no-cache-dir konlpy

# 애플리케이션 코드 복사
COPY . /app/

# 필요한 디렉토리 생성
RUN mkdir -p /app/.temp /app/.db /app/logs /app/audio

# 포트 노출
EXPOSE 8000

# 환경 변수 설정
ENV PYTHONPATH=/app

# 실행 명령
CMD ["python", "main_enhanced.py", "--cpu-only"] 